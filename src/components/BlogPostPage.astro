---
import Layout from '../layouts/Layout.astro';
import Navbar from './Navbar.astro';
import SiteFooter from './SiteFooter.astro';
import { blogUi, defaultLocale, type LocaleCode } from '../data/i18n/locales';
import {
	absoluteBlogUrl,
	getAllPostsForLocale,
	getBlogBasePath,
	getBlogPostHreflangAlternates,
} from '../data/blog/helpers';
import type { ResolvedBlogPost } from '../data/blog/types';
import { blogLabel, siteConfig } from '../data/site';
import { brandCopy } from '../data/site-core';
import { getLocalizedPath, localizeInternalHref } from '../data/i18n/routing';

interface Props {
	locale: LocaleCode;
	post: ResolvedBlogPost;
}

const { locale, post } = Astro.props;
const ui = blogUi[locale];
const t = post.translation;
const homeHref = getLocalizedPath('home', locale);
const blogHref = getBlogBasePath(defaultLocale);
const related = getAllPostsForLocale(defaultLocale)
	.filter((p) => p.id !== post.id)
	.slice(0, 3);

const articleUrl = absoluteBlogUrl(defaultLocale, post.translations[defaultLocale]?.slug ?? t.slug);
const hreflangAlternates = getBlogPostHreflangAlternates(post, locale);
const articleSchema = {
	'@type': 'Article',
	'@id': `${articleUrl}#article`,
	url: articleUrl,
	headline: t.title,
	description: t.metaDescription,
	datePublished: post.published,
	dateModified: post.updated,
	image: new URL(post.imageSrc, siteConfig.url).href,
	inLanguage: locale,
	author: { '@type': 'Organization', name: siteConfig.name, url: siteConfig.url },
	publisher: {
		'@type': 'Organization',
		name: siteConfig.name,
		url: siteConfig.url,
		logo: { '@type': 'ImageObject', url: new URL(siteConfig.logoRaster, siteConfig.url).href },
	},
	mainEntityOfPage: {
		'@type': 'WebPage',
		'@id': `${articleUrl}#webpage`,
	},
	isPartOf: { '@id': `${siteConfig.url}/#website` },
	keywords: t.keywords.join(', '),
	articleSection: post.category,
};

const faqSchema =
	t.faq && t.faq.length > 0
		? {
				'@type': 'FAQPage',
				'@id': `${articleUrl}#faq`,
				mainEntity: t.faq.map((item) => ({
					'@type': 'Question',
					name: item.question,
					acceptedAnswer: {
						'@type': 'Answer',
						text: item.answer.replace(/<[^>]+>/g, ''),
					},
				})),
			}
		: null;

const extraGraph = [articleSchema, faqSchema].filter(Boolean) as Record<string, unknown>[];
const storeHref = localizeInternalHref('/pricing/', locale);

function localizeParagraphHtml(html: string): string {
	return html.replace(/href="(\/[^"]+)"/g, (_, path: string) => {
		return `href="${localizeInternalHref(path, locale)}"`;
	});
}

/** Keep document titles within ~60 chars without cutting mid-word. */
function pageTitle(headline: string): string {
	const suffix = ` | ${blogLabel}`;
	if (headline.length + suffix.length <= 60) return `${headline}${suffix}`;
	if (headline.length <= 60) return headline;
	const trimmed = headline.slice(0, 57);
	const cut = trimmed.lastIndexOf(' ');
	return `${(cut > 40 ? trimmed.slice(0, cut) : trimmed).replace(/[|:–—-]+$/, '').trim()}…`;
}
---

<Layout
	title={pageTitle(t.title)}
	description={t.metaDescription}
	canonicalPath={post.canonicalPath}
	hreflangAlternates={hreflangAlternates}
	ogImage={post.imageSrc}
	ogImageAlt={t.imageAlt}
	type="article"
	locale={locale}
	breadcrumbs={[
		{ label: ui.home, href: homeHref },
		{ label: ui.blogH1, href: blogHref },
		{ label: t.h1 },
	]}
	extraGraph={extraGraph}
>
	<Navbar locale={locale} />
	<main class="post">
		<div class="shell">
			<div class="shell-content post__inner">
		<article>
			<header class="post__header">
				<p class="post__cat">{post.category}</p>
				<h1>{t.h1}</h1>
				<p class="post__intro">{t.intro}</p>
				<p class="post__meta">
					{ui.published} {post.published} · {ui.updated} {post.updated}
				</p>
			</header>
			{
				!post.hideHero && (
					<img
						class="post__hero"
						src={post.imageSrc}
						alt={t.imageAlt}
						width="1200"
						height="675"
						loading="eager"
						decoding="async"
					/>
				)
			}
			{
				t.sections.map((section) => (
					<section class="post__section">
						<h2>{section.h2}</h2>
						{section.paragraphs.map((paragraph) => {
							const isTip = paragraph.startsWith('Pro Tip');
							const isTry = paragraph.startsWith('Try This Today');
							const html = localizeParagraphHtml(paragraph);
							if (isTip || isTry) {
								return (
									<aside class:list={['callout', { 'callout--tip': isTip, 'callout--try': isTry }]}>
										<p set:html={html} />
									</aside>
								);
							}
							return <p set:html={html} />;
						})}
					</section>
				))
			}
			{
				t.faq && t.faq.length > 0 && (
					<section class="post__section post__faq">
						<h2>FAQ</h2>
						{t.faq.map((item) => (
							<div class="post__faq-item">
								<h3>{item.question}</h3>
								<p set:html={localizeParagraphHtml(item.answer)} />
							</div>
						))}
					</section>
				)
			}
		</article>

		<section class="post__cta" aria-label={`Buy ${siteConfig.name}`}>
			<h2>Get the full stack for {siteConfig.name}</h2>
			<p>
				Hero ESP, aimbot, and cooldown tracker in one Windows PC license. Check Status after
				patches, then queue.
			</p>
			<div class="post__cta-row">
				<a class="post__cta-buy" href={siteConfig.checkoutUrl} rel="noopener noreferrer">
					{brandCopy.ctaBuy}
				</a>
				<a class="post__cta-link" href={storeHref}>Open Store</a>
			</div>
		</section>

		{
			related.length > 0 && (
				<section class="post__related" aria-label={ui.relatedPosts}>
					<h2>{ui.relatedPosts}</h2>
					<ul>
						{related.map((item) => (
							<li>
								<a href={item.canonicalPath}>{item.translation.title}</a>
							</li>
						))}
					</ul>
				</section>
			)
		}
			</div>
		</div>
	</main>
	<SiteFooter locale={locale} />
</Layout>

<style>
	.post {
		padding: 2.5rem 0 4rem;
	}

	.post__inner {
		display: grid;
		gap: 0;
	}
	.post__cat {
		margin: 0 0 0.5rem;
		color: var(--magenta-soft);
		font-size: 0.75rem;
		font-weight: 700;
		letter-spacing: 0.08em;
		text-transform: uppercase;
	}
	.post__header {
		margin-bottom: 1.5rem;
	}
	.post__header h1 {
		margin: 0 0 0.75rem;
		font-size: clamp(1.9rem, 4vw, 2.8rem);
		line-height: 1.1;
	}
	.post__intro {
		color: rgba(255, 255, 255, 0.78);
		font-size: 1.08rem;
	}
	.post__meta {
		color: rgba(255, 255, 255, 0.5);
		font-size: 0.85rem;
	}
	.post__hero {
		width: 100%;
		aspect-ratio: 16 / 9;
		object-fit: cover;
		margin: 1.5rem 0 2rem;
		border: 1px solid rgba(255, 255, 255, 0.08);
	}
	.post__section {
		margin-bottom: 1.75rem;
	}
	.post__section h2 {
		margin: 0 0 0.75rem;
		font-size: 1.35rem;
	}
	.post__section h3 {
		margin: 1.15rem 0 0.45rem;
		font-size: 1.08rem;
		line-height: 1.35;
	}
	.post__faq-item h3 {
		color: rgba(255, 255, 255, 0.95);
	}
	.post__section p {
		margin: 0 0 0.9rem;
		color: rgba(255, 255, 255, 0.78);
		line-height: 1.7;
	}
	.callout {
		margin: 0 0 1rem;
		padding: 0.9rem 1rem;
		border-radius: var(--radius-md, 0.75rem);
		border: 1px solid color-mix(in srgb, var(--magenta) 35%, var(--line));
		background: color-mix(in srgb, var(--magenta) 10%, var(--bg-panel));
	}
	.callout--try {
		border-color: rgba(52, 211, 153, 0.4);
		background: rgba(52, 211, 153, 0.08);
	}
	.callout p {
		margin: 0;
		color: rgba(255, 255, 255, 0.92);
	}
	.post__cta {
		margin-top: 2rem;
		padding: 1.35rem 1.25rem;
		border-radius: var(--radius-md, 0.75rem);
		border: 1px solid color-mix(in srgb, var(--magenta) 40%, var(--line));
		background: color-mix(in srgb, var(--magenta) 12%, var(--bg-panel));
	}
	.post__cta h2 {
		margin: 0 0 0.5rem;
		font-size: 1.25rem;
	}
	.post__cta p {
		margin: 0 0 1rem;
		color: rgba(255, 255, 255, 0.78);
		line-height: 1.6;
	}
	.post__cta-row {
		display: flex;
		flex-wrap: wrap;
		gap: 0.65rem;
	}
	.post__cta-buy {
		display: inline-flex;
		align-items: center;
		justify-content: center;
		padding: 0.7rem 1.15rem;
		border-radius: 0.5rem;
		background: var(--magenta);
		color: #fff;
		font-weight: 700;
		text-decoration: none;
	}
	.post__cta-buy:hover {
		background: var(--magenta-hover, var(--magenta-soft));
	}
	.post__cta-link {
		display: inline-flex;
		align-items: center;
		justify-content: center;
		padding: 0.7rem 1.15rem;
		border-radius: 0.5rem;
		border: 1px solid rgba(255, 255, 255, 0.18);
		color: var(--magenta-soft);
		font-weight: 600;
		text-decoration: none;
	}
	.post__cta-link:hover {
		text-decoration: underline;
	}
	.post__related {
		margin-top: 3rem;
		padding-top: 1.5rem;
		border-top: 1px solid rgba(255, 255, 255, 0.1);
	}
	.post__related h2 {
		margin: 0 0 0.75rem;
		font-size: 1.15rem;
	}
	.post__related ul {
		margin: 0;
		padding: 0;
		list-style: none;
	}
	.post__related li {
		margin-bottom: 0.55rem;
	}
	.post__related a {
		color: var(--magenta-soft);
		text-decoration: none;
		font-weight: 600;
	}
	.post__related a:hover {
		text-decoration: underline;
	}
</style>
